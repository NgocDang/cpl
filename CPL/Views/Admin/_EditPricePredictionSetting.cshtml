@model PricePredictionSettingAdminViewModel

<div class="modal" tabindex="-1" role="dialog" id="edit-price-prediction" style="display: none;" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <form id="form-edit-price-prediction" method="POST" action="#" class="needs-validation" novalidate="">
                <div class="modal-header">
                    <h5 class="modal-title">@LangDetailHelper.Get(Context.Session.GetInt32("LangId").Value, "PricePrediction")</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"> <span aria-hidden="true">×</span> </button>
                </div>
                <div class="modal-body">
                    <div class="row">
                        <div class="col-lg-6 col-12">
                            <fieldset class="form-group">
                                <label for="price-prediction-category" class="form-title">@LangDetailHelper.Get(Context.Session.GetInt32("LangId").Value, "PricePredictionCategory")</label>
                                <select data-value="@Model.PricePredictionCategoryId" name="price-prediction-category" id="price-prediction-category" class="selectpicker" required data-style="btn" title="@LangDetailHelper.Get(Context.Session.GetInt32("LangId").Value, "PleaseSelect")">
                                    <option class="text-primary font-weight-bold" value="0">@LangDetailHelper.Get(Context.Session.GetInt32("LangId").Value, "CreateNewCategory")</option>
                                    @foreach (var category in Model.PricePredictionCategoryAdminViewModels)
                                    {
                                        <option value="@category.Id">@category.Name</option>
                                    }
                                </select>
                                <div class="invalid-feedback" id="category-msg">
                                    @LangDetailHelper.Get(Context.Session.GetInt32("LangId").Value, "PricePredictionCategoryRequired")
                                </div>
                            </fieldset>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-lg-3 col-12 pt-1">
                            <fieldset class="form-group">
                                <label for="ticket-price" class="form-title">Open Betting Time</label>
                                <input id="timepicker1" type="text" class="form-control input-small">
                                <span class="input-group-addon">
                                    <i class="glyphicon glyphicon-time"></i>
                                </span>
                            </fieldset>
                        </div>
                        <div class="col-lg-3 col-12 pt-1">
                            <fieldset class="form-group">
                                <label for="ticket-price" class="form-title">Close Betting Time</label>
                                <input id="timepicker2" type="text" class="form-control input-small">
                            </fieldset>
                        </div>
                        <div class="col-lg-6 col-12 pt-1">
                            <fieldset class="form-group">
                                <label for="holding-time-interval" class="form-title">Holding Time Interval</label>
                                <input name="holding-time-interval" id="holding-time-interval" type="number" min="1" value="@Model.HoldingTimeInterval" class="form-control" required autofocus />
                                <div class="invalid-feedback">
                                    @LangDetailHelper.Get(Context.Session.GetInt32("LangId").Value, "TicketPriceRequired")
                                </div>
                            </fieldset>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-lg-6 col-12 pt-1">
                            <fieldset class="form-group">
                                <label for="result-time-interval" class="form-title">Result Time Interval</label>
                                <input name="result-time-interval" id="result-time-interval" type="number" min="1" value="@Model.ResultTimeInterval" class="form-control" required autofocus />
                                <div class="invalid-feedback">
                                    @LangDetailHelper.Get(Context.Session.GetInt32("LangId").Value, "TicketPriceRequired")
                                </div>
                            </fieldset>
                        </div>
                        <div class="col-lg-6 col-12 pt-1">
                            <fieldset class="form-group">
                                <label for="divided-rate" class="form-title">Divided Rate</label>
                                <input name="divided-rate" id="divided-rate" type="number" min="1" value="@Model.HoldingTimeInterval" class="form-control" required autofocus />
                                <div class="invalid-feedback">
                                    @LangDetailHelper.Get(Context.Session.GetInt32("LangId").Value, "TicketPriceRequired")
                                </div>
                            </fieldset>
                        </div>
                    </div>
                    <div id="lottery-multilanguage" class="tab-component">
                        <ul class="nav nav-tabs border-0">
                            @foreach (var detail in Model.PricePredictionSettingDetailAdminViewModels)
                            {
                                <li class="nav-item">
                                    <a class="nav-link font-medium-2 font-weight-bold px-2 border-0 @(detail.LangId == Context.Session.GetInt32("LangId").Value ? "active" : "" )" id="@detail.LangId-price-prediction-nav-tab" data-toggle="tab" href="#@detail.LangId-price-prediction-nav" aria-controls="@detail.LangId-price-prediction-nav" aria-expanded="true">@detail.LangName</a>
                                </li>
                            }

                        </ul>
                        <div class="tab-content mb-1">
                            @foreach (var detail in Model.PricePredictionSettingDetailAdminViewModels)
                            {
                                <div role="tabpanel" class="tab-pane @(detail.LangId == Context.Session.GetInt32("LangId").Value ? "active" : "" )" id="@detail.LangId-price-prediction-nav" aria-labelledby="@detail.LangId-price-prediction-nav-tab" aria-expanded="true">
                                    <div class="card mb-1">
                                        <div class="card-content">
                                            <div class="card-body">
                                                <div class="row pt-1">
                                                    <input id="lang-id" type="hidden" value="@detail.LangId">
                                                    <div class="col-12">
                                                        <fieldset class="form-group">
                                                            <label for="title" class="form-title">Title</label> <span> ( @detail.LangName )</span>
                                                            <input name="title" id="title" type="text" min="1" value="@detail.Title" class="form-control" required autofocus />
                                                            <div class="invalid-feedback">
                                                                @LangDetailHelper.Get(Context.Session.GetInt32("LangId").Value, "TicketPriceRequired")
                                                            </div>
                                                        </fieldset>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
                <div class="modal-footer text-right">
                    <button type="button" class="btn btn-primary btn-sm btn-do-add">@LangDetailHelper.Get(Context.Session.GetInt32("LangId").Value, "Submit") <i class="la la-plus font-size-15px"></i></button>
                    <button type="button" class="btn btn-secondary btn-sm btn-do-cancel" data-dismiss="modal">@LangDetailHelper.Get(Context.Session.GetInt32("LangId").Value, "Cancel")</button>
                </div>
            </form>
        </div>
    </div>
</div>
<div id="modal-price-prediction-category"></div>
